/**
 * @param {number[][]} matrix
 * @param {number} target
 * @return {boolean}
 */
var searchMatrix = function(matrix, target) {
	if (target < martix[0][0] || target > matrix[matrix.length - 1][matrix[matrix.length - 1].length - 1]) {
		return false;
	}
	var found = false;
	var curRow = Math.floor(matrix.length / 2);
	var curCol = Math.floor(matrix.)
	while (!found) {
		
	}
};